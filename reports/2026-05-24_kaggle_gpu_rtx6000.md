# 2026-05-24 Kaggle RTX PRO 6000 Migration

## Status

Kaggle GPU usage is now verified for this project.

- Local Kaggle CLI push gave default `Tesla P100-PCIE-16GB`.
- Remote Kaggle CLI 2.1.0 push gave `NVIDIA RTX PRO 6000 Blackwell Server Edition`.
- The working path is to push RTX kernels from the remote server, not from the local Kaggle CLI.

No competition submission was consumed in this migration step.

## Daily State Checked

- Today used submissions visible before this GPU work: 0.
- Public leaderboard top remains 0.87, with many 0.86 ties.
- Latest public Code/Dataset signals were saved under `logs/2026-05-24/`.

Saved logs:

- `logs/2026-05-24/submissions_before_kaggle_gpu.csv`
- `logs/2026-05-24/latest_kernels_before_kaggle_gpu.txt`
- `logs/2026-05-24/latest_datasets_before_kaggle_gpu.txt`
- `logs/2026-05-24/leaderboard_before_kaggle_gpu.txt`

## Kernel Work

Probe kernel:

- `kaggle_kernels/nemotron_gpu_probe_may24/`
- Local push output: P100 16GB.
- Remote push output: RTX PRO 6000, 96GB.
- Successful report: `outputs/kaggle_gpu_probe_may24/v2_output/gpu_probe_report.json`

Training smoke kernel:

- `kaggle_kernels/nemotron_rtx6000_train_smoke_may24/`
- Kaggle kernel: `beicicc/nemotron-rtx6000-train-smoke-may24`
- Successful version: v4.
- Successful report: `outputs/kaggle_rtx_train_smoke_may24_v4/kaggle_rtx_train_report.json`

v4 loaded:

- Base model: `/kaggle/input/models/metric/nemotron-3-nano-30b-a3b-bf16/transformers/default/1`
- Init adapter: `/kaggle/input/models/kienngx/nemotron-nano-30b-trained/triton/tinker-adapter/1`
- Train CSV: `/kaggle/input/competitions/nvidia-nemotron-model-reasoning-challenge/train.csv`
- GPU: `NVIDIA RTX PRO 6000 Blackwell`, 96GB class.

v4 training smoke result:

- records: 8
- max length: 512
- trainable params: 888,154,112
- total params: 32,466,091,456
- train runtime for 2 steps: 14.16 seconds
- total kernel elapsed: 523.56 seconds
- status: ok

Fixes required for Kaggle RTX training:

- Add `ryanholbrook/nvidia-utility-script` as a kernel source.
- Add utility root and `nvidia_cutlass_dsl/python_packages` to Python path.
- Discover `train.csv` recursively under `/kaggle/input`; the working path is under `/kaggle/input/competitions/`.
- Copy `ptxas-blackwell` to `/tmp`, chmod executable, and set `TRITON_PTXAS_BLACKWELL_PATH` / `TRITON_PTXAS_PATH`.

## Next Direction

The next useful run should not be another blind public adapter submit. The viable Kaggle GPU path is:

1. Continue from the `kienngx` 0.86 Tinker adapter model source.
2. Use low learning rate, small curated official/hard-format data, and checkpointed short runs.
3. Add validation before producing a real `submission.zip`.
4. Submit only if validation beats the current 0.86 baseline meaningfully.

Suggested first formal Kaggle run:

- `SAVE_ADAPTER=True`
- `max_steps=16-64`
- `max_length=512`
- `lr=5e-7` to `1e-6`
- official boxed/hard rows first, not full random SFT.
